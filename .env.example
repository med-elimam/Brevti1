# Server
PORT=5000
NODE_ENV=development

# Admin panel authentication token
ADMIN_TOKEN=your-secret-admin-token

# OpenAI (fallback if Replit AI Integrations not available)
OPENAI_API_KEY=sk-...

# Mobile app API base URL
# On Replit: auto-detected via EXPO_PUBLIC_DOMAIN (no need to set this)
# On Railway/Fly/self-hosted: set to your deployed server URL
# EXPO_PUBLIC_API_BASE_URL=https://your-server.example.com
